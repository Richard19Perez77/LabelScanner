{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Metro (scan result)",
      "type": "node",
      "request": "attach",
      "port": 8081,
      "cwd": "${workspaceFolder}"
    }
  ]
}
